# TODO - Convert bun format to npm format

- [x] Inspect repo for Bun-specific configuration/scripts.
- [x] Update `package.json` to include Wrangler npm scripts (dev/build) instead of Bun.
- [x] Remove Bun artifacts `bun.lock` and `bunfig.toml`.

- [x] Run `npm ci`.
- [x] Run `npm run build`.
- [x] Run `npm run dev` (optional) to verify dev server starts.
